{
  "name": "zeywin-android-builder-api",
  "version": "1.0.0",
  "private": true,
  "description": "Dedicated API for ZeyWin Android APK builder form.",
  "scripts": {
    "dev": "vercel dev",
    "check": "node scripts/check.js"
  },
  "engines": {
    "node": ">=20"
  }
}
